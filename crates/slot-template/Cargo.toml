[package]
name = "slot-template"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
description = "pure-function slot templating: render(template, args) with strict, call-site-checked slots"
license = "MIT"
repository = "https://github.com/agent-habilis/agent-gossip"
publish = false

[lints]
workspace = true

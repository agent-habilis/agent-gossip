{
  "name": "agent-gossip-com",
  "private": true,
  "type": "module",
  "workspaces": [
    "*"
  ],
  "scripts": {
    "start": "bun scripts/build.ts && bun server/server.ts",
    "dev": "portless",
    "test": "bun test",
    "type-check": "tsc -p tsconfig.json && tsc --noEmit -p app && tsc --noEmit -p server && tsc --noEmit -p visage-dom && tsc --noEmit -p visage-style && tsc --noEmit -p visage-router && tsc --noEmit -p moonspace && tsc --noEmit -p moonspace-theme && tsc --noEmit -p moonspace-dom",
    "media": "./scripts/encode-media.sh",
    "docker:build": "docker compose build",
    "docker:up": "docker compose up -d",
    "docker:down": "docker compose down",
    "docker:logs": "docker compose logs -f",
    "docker:restart": "docker compose restart",
    "docker:ps": "docker compose ps",
    "docker:publish": "docker build -t ${DOCKER_IMAGE:-srvc-gitea.tetra-ostrich.ts.net/caiogondim/agent-gossip-com}:latest -t ${DOCKER_IMAGE:-srvc-gitea.tetra-ostrich.ts.net/caiogondim/agent-gossip-com}:$(git rev-parse --short HEAD) . && docker push ${DOCKER_IMAGE:-srvc-gitea.tetra-ostrich.ts.net/caiogondim/agent-gossip-com}:latest && docker push ${DOCKER_IMAGE:-srvc-gitea.tetra-ostrich.ts.net/caiogondim/agent-gossip-com}:$(git rev-parse --short HEAD)",
    "build": "bun scripts/build.ts",
    "serve": "bun server/server.ts",
    "e2e": "bun scripts/e2e.ts",
    "build:wasm": "bun scripts/build-wasm.ts"
  },
  "portless": {
    "name": "agent-gossip",
    "script": "start"
  },
  "engines": {
    "bun": ">=1.3.0"
  },
  "dependencies": {
    "visage-dom": "workspace:*",
    "visage-style": "workspace:*",
    "visage-router": "workspace:*",
    "moonspace": "workspace:*",
    "moonspace-theme": "workspace:*",
    "moonspace-dom": "workspace:*",
    "@agent-gossip/app": "workspace:*",
    "@agent-gossip/server": "workspace:*"
  },
  "devDependencies": {
    "@happy-dom/global-registrator": "^20.11.1",
    "@types/bun": "^1.3.14",
    "portless": "^0.15.5",
    "typescript": "^5.9.3"
  }
}

# Bun does not walk up for a bunfig, so `cd web && bun test` would otherwise run
# the webapp suites with no DOM registered. The root run picks up the root bunfig.
[test]
preload = ["../scripts/test-setup.ts"]

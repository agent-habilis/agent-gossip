[package]
name = "ahs-shared"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
publish = false

# Empty by design: a dependency-free leaf so xtask never links the daemon.
[dependencies]

[lints]
workspace = true
